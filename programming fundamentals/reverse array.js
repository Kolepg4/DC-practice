let a = [3,4,5,6,7,8];

let b = [];
let j = 0;

for(i=5; i> -1; i--){
    // b.push (a[i])
    b[j]=a[i]
    j=j + 1
}
console.log(b);

// b.push is pushing the value inside of the Array. Adding stuff one by one
// Push is a method that lets you add a method to an array